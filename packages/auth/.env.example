# Database connection (used by e2e tests and local dev)
DATABASE_URL=postgresql://test:test@localhost:5444/auth_test

# JWT secret (required — use a long random string in production)
JWT_SECRET=your-jwt-secret-here

# Optional: OAuth credentials (only needed if enabling OAuth features)
# GOOGLE_CLIENT_ID=your-google-client-id
# APPLE_CLIENT_ID=your-apple-client-id
