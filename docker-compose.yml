# Auto-generated by scripts/generate-all.js
# Do not edit directly - modify factiii.yml and re-run generator

services:
  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: infrastructure_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - certbot-www:/var/www/certbot:ro
      - certbot-conf:/etc/letsencrypt:ro
    networks:
      - infrastructure_network
    restart: unless-stopped
    depends_on:
      - certbot
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Certbot for SSL
  certbot:
    image: certbot/certbot
    container_name: infrastructure_certbot
    volumes:
      - certbot-www:/var/www/certbot
      - certbot-conf:/etc/letsencrypt
    networks:
      - infrastructure_network
    command: certonly --webroot --webroot-path=/var/www/certbot --email test@example.com --agree-tos --no-eff-email -d staging-test-repo.test.example.com -d test.example.com -d staging-another-repo.test.example.com
    restart: "no"

  # Postgres Staging Database
  postgres-staging:
    image: postgres:15-alpine
    container_name: infrastructure_postgres_staging
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
    volumes:
      - postgres-staging-data:/var/lib/postgresql/data
    networks:
      - infrastructure_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # test-repo - staging
  test-repo-staging:
    image: 123456789.dkr.ecr.us-east-1.amazonaws.com/test-apps:test-repo-main-latest
    container_name: test-repo-staging
    environment:
      - NODE_ENV=staging
    env_file:
      - ./secrets/test-repo-staging.env
    networks:
      - infrastructure_network
    restart: unless-stopped
    depends_on:
      postgres-staging:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # test-repo - prod
  test-repo-prod:
    image: 123456789.dkr.ecr.us-east-1.amazonaws.com/test-apps:test-repo-main-latest
    container_name: test-repo-prod
    environment:
      - NODE_ENV=production
    env_file:
      - ./secrets/test-repo-prod.env
    networks:
      - infrastructure_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # another-repo - staging
  another-repo-staging:
    image: 123456789.dkr.ecr.us-east-1.amazonaws.com/test-apps:another-repo-main-latest
    container_name: another-repo-staging
    environment:
      - NODE_ENV=staging
    env_file:
      - ./secrets/another-repo-staging.env
    networks:
      - infrastructure_network
    restart: unless-stopped
    depends_on:
      postgres-staging:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  postgres-staging-data:
  certbot-www:
  certbot-conf:

networks:
  infrastructure_network:
    driver: bridge
