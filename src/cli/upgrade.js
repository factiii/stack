const fs = require('fs');
const path = require('path');
const yaml = require('js-yaml');
const { execSync } = require('child_process');

/**
 * Get current Core package version
 */
function getCoreVersion() {
  try {
    const packageJsonPath = path.join(__dirname, '../../package.json');
    const pkg = JSON.parse(fs.readFileSync(packageJsonPath, 'utf8'));
    return pkg.version || '1.0.0';
  } catch (e) {
    return '1.0.0';
  }
}

/**
 * Get latest version from npm registry
 */
async function getLatestVersion() {
  try {
    const result = execSync('npm view @factiii/core version 2>/dev/null', {
      encoding: 'utf8',
      stdio: 'pipe'
    }).trim();
    return result || null;
  } catch (e) {
    return null;
  }
}

/**
 * Parse semantic version
 */
function parseVersion(version) {
  const match = version.match(/^(\d+)\.(\d+)\.(\d+)/);
  if (!match) return null;
  return {
    major: parseInt(match[1], 10),
    minor: parseInt(match[2], 10),
    patch: parseInt(match[3], 10)
  };
}

/**
 * Compare versions: returns -1, 0, or 1
 */
function compareVersions(a, b) {
  const vA = parseVersion(a);
  const vB = parseVersion(b);
  
  if (!vA || !vB) return 0;
  
  if (vA.major !== vB.major) return vA.major - vB.major;
  if (vA.minor !== vB.minor) return vA.minor - vB.minor;
  return vA.patch - vB.patch;
}

/**
 * Check if upgrade is breaking (major version change)
 */
function isBreakingUpgrade(from, to) {
  const vFrom = parseVersion(from);
  const vTo = parseVersion(to);
  
  if (!vFrom || !vTo) return false;
  return vTo.major > vFrom.major;
}

/**
 * Read coreAuto.yml version info
 */
function readCoreAutoVersion(rootDir) {
  const coreAutoPath = path.join(rootDir, 'coreAuto.yml');
  
  if (!fs.existsSync(coreAutoPath)) {
    return { core_version: null, core_min_version: null };
  }
  
  try {
    const content = fs.readFileSync(coreAutoPath, 'utf8');
    const config = yaml.load(content);
    return {
      core_version: config.core_version || null,
      core_min_version: config.core_min_version || null
    };
  } catch (e) {
    return { core_version: null, core_min_version: null };
  }
}

/**
 * Upgrade command
 */
async function upgrade(options = {}) {
  const rootDir = process.cwd();
  const currentVersion = getCoreVersion();
  const coreAutoVersion = readCoreAutoVersion(rootDir);
  
  console.log('═'.repeat(70));
  console.log('🔄 Core Upgrade Check');
  console.log('═'.repeat(70));
  console.log('');
  
  // Show current versions
  console.log('📊 Version Information:\n');
  console.log(`   Installed Core version: ${currentVersion}`);
  
  if (coreAutoVersion.core_version) {
    console.log(`   coreAuto.yml generated by: ${coreAutoVersion.core_version}`);
    
    if (compareVersions(currentVersion, coreAutoVersion.core_version) !== 0) {
      console.log(`   ⚠️  Version mismatch detected`);
    } else {
      console.log(`   ✅ Versions match`);
    }
  } else {
    console.log(`   coreAuto.yml: No version info (legacy file)`);
  }
  
  console.log('');
  
  // Check for updates
  if (options.check) {
    console.log('🔍 Checking for updates...\n');
    
    const latestVersion = await getLatestVersion();
    
    if (latestVersion) {
      console.log(`   Latest version on npm: ${latestVersion}`);
      
      const comparison = compareVersions(latestVersion, currentVersion);
      
      if (comparison > 0) {
        console.log(`   📦 Update available: ${currentVersion} → ${latestVersion}`);
        
        if (isBreakingUpgrade(currentVersion, latestVersion)) {
          console.log(`   ⚠️  This is a MAJOR version upgrade`);
          console.log(`      Check CHANGELOG.md for breaking changes`);
        }
        
        console.log('');
        console.log('   To upgrade:');
        console.log('   npm update @factiii/core');
        console.log('   npx core upgrade');
      } else {
        console.log(`   ✅ You have the latest version`);
      }
    } else {
      console.log('   ⚠️  Could not check npm registry');
      console.log('      (Package may not be published yet)');
    }
    
    console.log('');
    return;
  }
  
  // Perform upgrade (regenerate configs)
  console.log('🔧 Regenerating configurations...\n');
  
  // Check if coreAuto.yml needs update
  const needsRegeneration = !coreAutoVersion.core_version || 
    compareVersions(currentVersion, coreAutoVersion.core_version) !== 0;
  
  if (needsRegeneration) {
    console.log('   Regenerating coreAuto.yml...');
    
    try {
      const { generateCoreAuto } = require('../generators/generate-core-auto');
      generateCoreAuto(rootDir);
      console.log('   ✅ coreAuto.yml updated\n');
    } catch (e) {
      console.error(`   ❌ Failed to regenerate coreAuto.yml: ${e.message}\n`);
    }
  } else {
    console.log('   ✅ coreAuto.yml is up to date\n');
  }
  
  // Regenerate workflows
  console.log('   Regenerating workflows...');
  
  try {
    const generateWorkflows = require('./generate-workflows');
    generateWorkflows({ output: '.github/workflows' });
  } catch (e) {
    console.error(`   ❌ Failed to regenerate workflows: ${e.message}\n`);
  }
  
  console.log('');
  console.log('═'.repeat(70));
  console.log('✅ Upgrade Complete');
  console.log('═'.repeat(70));
  console.log('');
  console.log('📋 Next steps:');
  console.log('   1. Review changes: git diff');
  console.log('   2. Commit: git add -A && git commit -m "Upgrade Core to ' + currentVersion + '"');
  console.log('   3. Run: npx core init (to verify)');
  console.log('');
}

module.exports = upgrade;

