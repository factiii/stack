# Environment files - production secrets must be gitignored
.env.prod

# Optional: Gitignore staging if sensitive (controlled by auto.isStagingSecret in factiii.yml)
# .env.staging

# Keep .env.example committed as template
# .env.example should be committed to show required keys

# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production builds
build/
dist/
.next/
out/

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files (other formats)
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Prisma
prisma/*.db
prisma/*.db-journal

# OS
.DS_Store
Thumbs.db

