---
"@factiii/stack": patch
---

Simplify vault export/import to copy/paste flow. Export prints vault key to screen instead of creating encrypted bundle file. Import accepts pasted key, prompts for personal passphrase, and saves wrapped as STACKVAULT1. Removes STACKEXPORT1 format.
