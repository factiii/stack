# Repos and secrets - ignore contents but keep directories
repos/*
!repos/.gitignore
secrets/*
!secrets/.gitignore

# Auto-generated files (created by scripts/generate-all.js)
docker-compose.yml
nginx/nginx.conf
infrastructure-config.yml

# Manual test files (gitignored - for local testing)
test-manual.yml

# Test temp files
test/temp-*
test/**/temp-*

# GitHub Actions runner (contains binaries and config)
actions-runner/

# Standard ignores
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/
*.log
.env.local
.env.*.local

# npm config (contains auth tokens)
.npmrc

# Node.js
node_modules/
package-lock.json

# TypeScript build output
dist/

# Docker
.dockerignore

# Backup files
*.bak
*.backup

# Machine-specific infrastructure config (staging vs prod)
infra.conf